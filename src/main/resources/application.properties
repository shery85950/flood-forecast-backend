# Server Configuration
server.port=8080

# Database Configuration (H2 - Default for Development)
spring.datasource.url=jdbc:h2:mem:flooddb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# Database Configuration (MySQL - Uncomment for Production)
# spring.datasource.url=jdbc:mysql://localhost:3306/flood_db?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=root
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.floodresponse=DEBUG

# AI Forecast Automation
grok.api.key=${GROK_API_KEY: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}
grok.api.url=https://api.x.ai/v1/chat/completions
grok.model=grok-beta
weather.api.key=${WEATHER_API_KEY:5523bf8add464255b93210055252911}
weather.api.url=https://api.weatherapi.com/v1

# Scheduling (cron: every Monday at 6 AM)
forecast.automation.cron=0 0 6 * * MON
forecast.automation.enabled=true

# Regions to monitor
forecast.regions=Punjab,Sindh,KPK,Balochistan
